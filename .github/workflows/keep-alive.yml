name: Keep Backend Alive

on:
  schedule:
    # Run every 14 minutes to prevent 15-minute sleep
    - cron: '*/14 * * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend
        run: |
          curl -I https://portfolio-ujx8.onrender.com/
          echo "Pinged backend to keep it awake"
